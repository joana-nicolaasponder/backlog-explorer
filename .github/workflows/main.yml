name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop, deploy, 'feature/*' ]
  pull_request:
    branches: [ main, develop, deploy ]

jobs:
  branch-check:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v3
      
      - name: Check branch naming and merge rules
        run: |
          # Get the target (base) branch and source branch
          TARGET_BRANCH="${{ github.base_ref }}"
          SOURCE_BRANCH="${{ github.head_ref }}"
          
          # Fail if trying to merge directly to main/deploy without going through develop
          if [[ "$TARGET_BRANCH" == "main" || "$TARGET_BRANCH" == "deploy" ]] && [[ "$SOURCE_BRANCH" != "develop" ]]; then
            echo "❌ Cannot merge directly to $TARGET_BRANCH. Please merge through develop first."
            exit 1
          fi
          
          # Ensure feature branches are properly named
          if [[ "$TARGET_BRANCH" == "develop" && ! "$SOURCE_BRANCH" =~ ^feature/ ]]; then
            echo "❌ Feature branches should start with 'feature/'. Current branch: $SOURCE_BRANCH"
            exit 1
          fi

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_RAWG_API_KEY: ${{ secrets.VITE_RAWG_API_KEY }}
          VITE_API_URL: ${{ secrets.VITE_API_URL }}

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run lint
        run: npm run lint

  pr-reminder:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: PR Checklist Reminder
        run: |
          echo "🔍 Review Checklist:"
          echo "✓ Code follows project style guidelines"
          echo "✓ Tests added/updated (if applicable)"
          echo "✓ Documentation updated (if applicable)"
          echo "✓ Commit messages are clear and descriptive"
          echo "✓ Branch is up to date with target branch"
